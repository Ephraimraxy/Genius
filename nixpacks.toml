[phases.setup]
nixPkgs = ["nodejs_22", "chromium"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "export PUPPETEER_EXECUTABLE_PATH=$(which chromium) && npm run start"
